
input(int())//int型の入力
input(float())//float型の入力
input(str())//str型の入力
a=int(input(''))//整数の入力をaに代入
b=float(input(''))//実数の入力をbに代入
----------------------------------------------------------

type(x) //xの型を返す
int(x)  //xを整数に変換
float(x)//xを実数に変換
str(x)  //xを文字列に変換
len(x)  //xの長さを返す（文字列、リスト、辞書などに使える）
----------------------------------------------------------
print('合計は'c) //「合計は１」と表示（ｃ＝１）
print('A\nB\nB')//改行
----------------------------------------------------------
N = int(input("繰り返しの回数Nを入力してください: "))
---------------------------------------------------------------------------------
random.choice([a,b,c])//a,b,cの中からランダムに１つ返す
random.uniform(a,b)//(a,b)で実数をランダムに返す
random.randint(a,b)//(a,b)で整数をランダムに返す
----------------------------------------------------------
for i in range(X)://これの下の行は頭の空白をそろえればすべてループに含まれる
      ~
----------------------------------------------------------
x={"1":"A","2":"B","3":"C"}
x["1"]//Aを返す
x.update({"4":"D"})//xに4:Dを追加
list(x.keys())//xのキーをリストで返す
list(x.values())//xの値をリストで返す
x.items()//xのキーと値をタプルで返す
A=[1,2,3,4,5]
A.append(6)//Aの末尾に6を追加
A.insert(0,0)//Aの先頭に0を追加
A.pop()//Aの末尾を削除
A.pop(0)//Aの先頭を削除
c=A.copy()//Aをコピーしてcに代入
A=[1,2,3,4,5]
B=[2,3,4,6,7,8,9,10]    
set(A) & set(B)//AとBの共通部分を返す
A+B//AとBを結合
A.sort()//Aを昇順に並べ替え
A.count(2)//Aの中に2が何個あるか
A.count()//Aの要素が何個あるか
A.index(2)//Aの中で2が何番目か
A.remove(2)//Aの中から2を削除
A.clear()//Aの中身を全て削除
----------------------------------------------------------
rondom.choice([a,b,c])//a,b,cの中からランダムに１つ返す
rondom.uniform(a,b)//(a,b)で実数をランダムに返す
rondom.randint(a,b)//(a,b)で整数をランダムに返す
import random
import math
import datetime
----------------------------------------------------------




